using UnityEngine;
using System.Collections;

public class ChangeTexture : MonoBehaviour
{
    public Texture redTexture;

	void Start()
    {
        //if(GetComponent<NetworkLobbyInfo>().characterIndex == 0)
            //GetComponent<Renderer>().material.mainTexture = redTexture;
    }
}
